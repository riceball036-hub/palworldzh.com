# PalworldZH 玩家体验、数据可信度与 SEO 审核（2026-09-05）

> 状态：**仅审核分支，禁止直接作为上线记录。** 目标分支：`audit/player-experience-seo-20260905`。本轮不合并 `main`、不触发生产部署。

## 审核目标

从两类真实玩家任务重新审视站点：

1. **第一次玩 / 回坑玩家**：能否不用先理解大量术语，就找到开局、第一基地、基础战斗和下一步推进路线。
2. **资深 / 后期玩家**：能否快速查到图鉴、配种、技能、掉落、Boss、科技和基地效率相关数据，并知道这些数据适用于哪个游戏版本。

## 本轮已修复的高优先级问题

### P0：站点身份和数据口径

- 删除首页“官方资料库”表述，统一为“玩家自制中文攻略与数据库”。
- 明确声明本站与 Pocketpair / Palworld 官方无隶属或授权关系。
- 修正“299 只帕鲁”的误导口径：Palworld 1.0 官方公布的帕鲁总数为 **287 种**；本站约 299 个为资料条目口径，其中另含部分形态、联动或特殊实体。
- 修复首页损坏的 WebSite JSON-LD。

### P0：版本可信度

- 全站加入统一的 **v1.0.3 数据基准**提示。
- 明确提醒：1.0 后配种、伙伴技能、科技、基地与战斗平衡都有较大变化，旧攻略不能默认继续正确。
- 新建“关于本站 / 数据来源与版本政策”，说明资料冲突时应以游戏内与官方更新为准。
- 新闻页不再把未重新核验的旧消息继续作为当前重点推荐。

### P1：新手与资深玩家信息架构

- 首页从“把所有栏目平铺”改为两条明确路径：
  - 新手：开局 30 分钟 → 第一基地 → 科技 → 基础战斗 → 什么时候开始配种。
  - 资深：图鉴 → 配种计算器 → 被动技能 → Boss / 掉落 / 地图。
- 新增 `getting-started.html`，采用少剧透的新手推进方式，避免一上来用终局配装和多代配种淹没新玩家。
- 首页核心入口缩成新手攻略、帕鲁图鉴、配种计算器、地图四个高频任务。

### P1：全站搜索

原仓库的 `js/search.js` 会请求不存在的 `/data/search-index.json`，导致全站搜索静默失败；首页输入时还会反复清空正在输入的关键词。

本轮修复：

- 恢复 `data/search-index.json` 基础搜索索引。
- 首页输入内容会被带入搜索面板，不再清空。
- 支持 Ctrl/Cmd + K、Esc、上下键与 Enter。
- 搜索结果输出增加 HTML 转义，避免搜索数据被直接拼接进 DOM。
- 图鉴支持 `paldex.html?q=关键词`，便于把搜索意图继续交给完整图鉴筛选。

> 当前全站轻量索引优先覆盖核心栏目和高频帕鲁；完整 299 个资料条目的名称查询仍由图鉴页自身筛选负责。

### P1：SEO 基础设施

原仓库没有 `robots.txt` 和 `sitemap.xml`，但页脚已经链接到 sitemap，实际形成死链。

本轮补充：

- `robots.txt`
- 核心页面 XML sitemap
- 新增的 v1.0.3 与 2026 人气投票页面已加入 sitemap
- 首页 canonical / OG / 合法的 WebSite schema
- 新手攻略 Article schema
- 新闻页 NewsArticle schema
- `_headers`：基础安全响应头与更合理的静态资源缓存策略

### P1：可访问性与移动端

- 增加“跳到正文”键盘链接。
- 移动端菜单支持正确的 `aria-expanded` 与 Esc 关闭。
- 版本提示与首页新信息架构均做移动端单列布局。

## 本轮新增 / 重点修改文件

- `index.html`
- `about.html`
- `getting-started.html`（新增）
- `news.html`
- `news/palworld-1-0-3.html`（新增）
- `news/palworld-popularity-poll-2026.html`（新增）
- `js/main.js`
- `js/search.js`
- `css/experience.css`（新增）
- `data/search-index.json`（新增）
- `robots.txt`（新增）
- `sitemap.xml`（新增）
- `_headers`（新增）

## 合并前仍需继续验证的事项

这些项目**本轮没有假装已经完成**：

1. **配种表的 1.0.3 全量数值复核**：仓库是线上 Cloudflare Pages 的静态镜像，`breed-calc.html` 等页面包含大量直接生成的数据，但仓库中没有看到对应的原始生成数据管线。没有可靠源数据时，不应盲目批量改配种结果。本轮先加版本风险提示。
2. **299 个详情条目的逐字段复核**：图鉴中的属性、工作适性、技能、掉落、科技等需要从真正的源数据批量校验，而不是手改数百个生成 HTML。
3. **完整详情 sitemap 自动生成**：当前 sitemap 先覆盖核心栏目与已核验内容；在拿到生成源后，应由构建脚本自动纳入全部帕鲁详情页，并保证 URL 编码一致。
4. **全量搜索索引自动生成**：当前恢复的是核心搜索能力；理想状态应由源数据自动生成所有帕鲁、物品、技能、Boss 的搜索词与别名。
5. **旧新闻逐篇事实复核**：未在本轮重新核验的旧新闻保留在仓库，但不再在版本中心作为当前推荐。
6. **真实浏览器回归测试**：这个仓库当前没有自动化 UI 测试或构建流程。合并前建议在预览环境检查桌面/手机导航、搜索、图鉴筛选、配种工具和 404。

## 合并 / 部署规则

- 本审核应通过 Draft PR 审阅。
- **不要直接合并到 `main`。**
- 在全量数据和浏览器预览确认前，不应将本分支作为生产发布。
