# PalworldZH 可用性审核（2026-09-05）

本轮目标不是单纯“变好看”，而是让新手能更快找到入口、熟练玩家能更快查数据，并减少搜索、筛选、移动端导航和长页面浏览中的阻力。

## 本轮已修复

### P0：搜索不可可靠使用
- 原 `js/search.js` 依赖 `data/search-index.json`，但仓库中没有该文件，完整搜索无法正常工作。
- 首页搜索框输入时会立刻打开覆盖层，同时旧逻辑把搜索框清空，导致首个输入字符丢失。
- 已改为：内置核心页面索引，并首次搜索时从同源的图鉴、物品、技能、被动技能页面懒加载名称索引；结果按相关度分组，使用 `sessionStorage` 缓存。
- 首页输入内容会原样带入搜索覆盖层；支持 Ctrl/Cmd+K、方向键、Enter、Esc。

### P0：导航状态和移动端操作
- 顶部主导航 + 工具导航入口很多，桌面端容易换行变高，移动端也缺少清晰的关闭行为。
- 工具页面的当前状态不准确，例如捕获率估算页仍把“帕鲁图鉴”标成当前页。
- 已改为：工具栏横向滚动、自动识别当前工具页、移动端主菜单网格化；点击菜单项、点击外部区域或按 Esc 均可关闭。

### P1：首页不知道“下一步去哪”
- 原首页按资料类型陈列功能，对熟悉站点的人尚可，但新手需要先理解“图鉴 / 配种 / 工作 / Boss / 地图”各自是什么。
- 已增加“你现在想做什么？”任务入口，按五个常见目标直达：认帕鲁、配指定帕鲁、找基地工人、打 Boss、找地点/资源。
- 首页不再使用“官方资料库”表述，避免与页脚“玩家自制”身份冲突。

### P1：筛选反馈不足
- 图鉴和物品页筛选后没有明确显示剩余结果数，也没有一键恢复默认状态。
- 已增加当前结果数量与“清除筛选”。
- 修复物品分类筛选的空状态计数：隐藏分类的匹配行不再被计入可见结果。
- 支持 `?q=关键词` 直接打开图鉴/物品/技能/被动技能页并自动执行页内搜索。

### P1：长页面浏览成本高
- 为长表格增加键盘可聚焦和横向滚动区域语义。
- 长页增加回到顶部按钮。
- 补充明显的键盘焦点样式和 `prefers-reduced-motion` 支持。
- 手机端核心入口由拥挤的两列改为更易点击的单列，并扩大常用触控目标。

### P1：明显的信任/一致性问题
- 首页 JSON-LD 原先格式损坏，已修复为合法 `schema.org` WebSite 数据。
- 首页 canonical / OG URL 统一到站点根路径。
- 全站可见属性标签统一为玩家更自然的“无 / 火 / 水 / 草 / 雷 / 冰 / 地 / 暗 / 龙”，避免“叶子 / 地球 / 黑暗”等翻译腔标签。
- 页脚链接到了缺失的 `sitemap.xml`，已补充基础站点地图，避免直接 404。

## 审核发现，但本轮没有贸然批量改动

### 1. 配种计算器单页体积约 4.4 MB
`breed-calc.html` 把大量数据直接塞进单个 HTML，首次载入、手机网络、浏览器解析和后续维护成本都很高。建议下一轮把配种数据拆为独立 JSON/JS 数据文件，页面只加载 UI 与必要索引，并按查询懒加载/缓存。

### 2. 部分帕鲁详情存在文本和结构化数据自相矛盾
例如抽查阿努比斯页面：顶部显示“史诗”，玩家点评段却写成“传说（数值 10）”；属性名称也曾出现“地球”。这不是样式问题，而是生成数据/文案模板的一致性问题。建议从源数据统一稀有度、属性、工作适性和点评字段，再批量重建详情页，而不是逐页手改。

### 3. 攻略页的机制性陈述应单独做数据真实性审核
配种、捕获率等页面包含公式、倍率和机制描述。可用性优化不能替代游戏版本验证；应以当前版本数据源核验后再改，避免“界面更好用但答案不可靠”。

### 4. 静态页头/页脚大量重复
目前大量 HTML 各自复制导航、工具栏和页脚。短期通过 `main.js + ux.css` 可以统一交互层，但长期应有生成模板或构建脚本，避免某些页面 active 状态、链接和版本文字悄悄漂移。

### 5. sitemap 目前只覆盖核心页面
本轮先修复页脚 404。完整 SEO sitemap 应由站点生成流程自动包含全部帕鲁详情页，并在每次数据重建时同步更新。

## 建议的下一轮顺序

1. 拆分 `breed-calc.html` 的 4.4 MB 内联数据，降低首屏和解析成本。
2. 对全部帕鲁详情跑一致性校验：编号、属性、稀有度、工作适性、技能、掉落、配种、点评互相核对。
3. 对配种/捕获/战斗等机制页做当前版本真实性核验。
4. 将重复的 header / tool bar / footer 纳入统一生成模板。
5. 再做真实手机尺寸和慢速网络下的交互/性能回归测试。

## 发布说明

本轮所有修改均提交到独立分支 `ux/usability-audit-20260905`，未合并 `main`，因此不应作为生产版本发布。
